{
  "name": "tramnew",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright test setup for GitHub Codespaces",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "^5.8.3"
  }
}
